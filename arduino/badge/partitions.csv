# Custom 4MB layout for the DEF CON 34 badge.
#
# `factory` is a small, immutable recovery application. Normal operation runs
# from the single large `main` slot. For OTA, main records the URL in NVS and
# boots recovery; recovery downloads directly into main, verifies it, and only
# then selects it for boot. If the new application fails its first boot, ESP32
# rollback returns to recovery, which keeps the failure visible on the panel.
#
# This trades instant rollback to the previous full firmware for substantially
# more feature space while preserving power-loss recovery. USB production
# flashing always writes both applications.
#
# Artwork remains independent in LittleFS. NVS stays byte-identical to the old
# layout, so repartitioning does not erase settings or badge identity.
#
# Name,     Type, SubType,  Offset,   Size,     Flags
nvs,        data, nvs,      0x9000,   0x5000,
otadata,    data, ota,      0xe000,   0x2000,
factory,    app,  factory,  0x10000,  0x110000,
main,       app,  ota_0,    0x120000, 0x210000,
storage,    data, littlefs, 0x330000, 0xC0000,
coredump,   data, coredump, 0x3F0000, 0x10000,
