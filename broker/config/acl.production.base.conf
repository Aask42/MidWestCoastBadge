# Fleet operator: signed commands, OTA, and complete diagnostics.
user operator
topic readwrite dc34/#
topic read $SYS/#

# Public static-site credential. Owner messages still require the physical
# badge pairing code and session-key proof.
user web
topic readwrite dc34/claim/#
topic write dc34/badge/+/owner
topic read dc34/badge/+/state

# Container health probe only.
user health
topic read $SYS/broker/uptime

# Per-badge blocks are appended below by scripts/enroll-badge.sh.